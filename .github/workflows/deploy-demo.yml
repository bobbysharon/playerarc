# Publishes the browser demonstration to GitHub Pages.
#
# One-time setup: in the repository, open Settings → Pages and set
# "Source" to "GitHub Actions". Then push to main.
#
# If your repository is not named "playerarc", change VITE_BASE below to
# match — a project page is served from /<repository-name>/.

name: Deploy demo to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build the demo
        run: npm run build:demo
        env:
          VITE_DEMO_MODE: 'true'
          VITE_BASE: /${{ github.event.repository.name }}/

      - name: Add .nojekyll
        run: touch web/dist/.nojekyll

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
