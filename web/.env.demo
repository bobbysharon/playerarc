# Used by: npm run build:demo
# Builds the browser-only version for a static host such as GitHub Pages.
VITE_DEMO_MODE=true
# Base path for a GitHub project page. Change if your repository is not named "playerarc".
VITE_BASE=/playerarc/
